# RQ2 (sentiment) dependencies — NO gensim required.
# RQ2 is self-contained (rq2/build_corpus.py mirrors RQ1's text filters), so it
# avoids gensim entirely. This matters on Python 3.13/3.14, where gensim has no
# prebuilt wheel and fails to compile from source.
pandas>=1.5.0
numpy>=1.20.0
scipy>=1.7.0
vaderSentiment>=3.3.2
matplotlib>=3.5.0
seaborn>=0.11.0
